PREFIX dc: <http://purl.org/dc/elements/1.1/>
SELECT ?title (replace("abcd", "b", "Z") as ?replace)
 WHERE { ?x dc:title  "That Seventies Show"@en ;
            dc:title  ?title .
         FILTER langMatches( lang(?title), "FR" ) } order by ?title