PREFIX ex: <http://people.example/>
SELECT ?y ?minName
WHERE {
    ex:alice ex:knows ?y .
    {
        SELECT ?y (MIN(?name) AS ?minName)
        WHERE {
            ?y ex:name ?name .
        } GROUP BY ?y
    }
}